# Local-Global Shared Infrastructure

This package is retained for its tested context-source, windowing, alignment,
pose metric, split, and artifact contracts. The completed ScanNet-50
local-global study is not rerun or republished from this worktree.

Multiscale code may reuse these modules but must keep its runners, schemas, and
exports under `pre_experiments/camera_refiner_data_construction/`.
